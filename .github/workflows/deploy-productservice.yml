name: Deploy ProductService to AWS ECR

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Debug workspace
        run: |
          echo "📁 Current working directory: $(pwd)"
          echo "📄 Listing all files and folders recursively:"
          ls -R
